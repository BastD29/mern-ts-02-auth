const BASE_URL = "http://localhost:3000/api";

const AUTH = "/auth";

const REGISTER = "/register";
const LOGIN = "/login";
const ME = "/me";
const LOGOUT = "/logout";
const LOGOUTALL = "/logoutall";

export { BASE_URL, AUTH, REGISTER, LOGIN, ME, LOGOUT, LOGOUTALL };
